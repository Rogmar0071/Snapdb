name: Build Android APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout repo
      - name: Checkout repo
        uses: actions/checkout@v4

      # Setup Node 22 (REQUIRED by Capacitor 8)
      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      # Install dependencies
      - name: Install deps
        run
